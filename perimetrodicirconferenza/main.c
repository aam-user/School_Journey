#include <stdio.h>
#include <math.h>
#define PI 3.1415926535

int main (){
    int raggio, area, perimetro;
    scanf("%d", & raggio);
    raggio=sqrt(area/PI);
    perimetro=2*PI*raggio;
    printf("Il perimetro e %d\n", perimetro);
    return 0;
}
